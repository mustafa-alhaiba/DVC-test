# DVC-test

## Download the data

After cloning the repository, run `dvc pull` to download the datasets tracked by DVC into the `data/` folder. This fetches the exact versioned files from the remote configured in `.dvc/config`.
